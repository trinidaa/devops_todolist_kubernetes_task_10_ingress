## Create cluster from kind and call all infrastructure scripts with check already exists.  :
```bash
./new-todoapp.sh
```
## To run cluster & infrastructure(all without check: "already exist"):
```bash
./bootstrap.sh
```